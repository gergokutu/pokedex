const calc = (totalItems: number, currentPage: number, itemsPerPage): number => {
  // current page is the last > return remaining items
  // return default page size
  // amount of itemson the current page
  return 1;
};